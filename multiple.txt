using System;
    class Rectangle
    {// keerthana learning branching
	    //changed from s3
        protected double l;
        protected double width;
		//changed from s3 second time
        public Rectangle(double s, double g)
        {
            length = l;
            width = w;
        }
		//changed third time from s3
        public double GetRectangle()
        {
            return length * width;
        }
		//changed fourth time from s3 if any issue found in s2 develoepers fix and put to s3 from there merge happens in s2 and ci in s2
        public void Display()
        {
            Console.WriteLine("Length: {0}", length);
            Console.WriteLine("Width: {0}", width);
            Console.WriteLine("Area: {0}", GetArea());
        }
    } 
    class Tabletop : Rectangle
    {
        private double cost;
        public Tabletop(double l, double w)
            : base(l, w)
        { }
        public double costcal()
        {
            double cost;
            cost = GetArea() * 70;
            return cost;
        }
        public void Display()
        {
            base.Display();
            Console.WriteLine("Cost: {0}", costcal());
        }
    }
    class CalRectangle
    {
        static void Main(string[] args)
        {
            Tabletop t = new Tabletop(7.5, 8.03);
            t.Display();
            Console.ReadLine();
        }
    }
